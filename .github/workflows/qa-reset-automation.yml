name: update-qa-after-release
on:
  push:
    branches: [ master ]
jobs:
  update-qa:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: | 
      git checkout master
      git fetch --all
      git checkout qa
      git merge --ff-only origin/main
      git push 

    