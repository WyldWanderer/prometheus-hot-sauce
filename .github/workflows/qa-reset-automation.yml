name: update-qa-after-release
on: push
jobs:
  update-qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git checkout master
      - run: git fetch --all
      - run: git checkout qa
      - run: git merge master --allow-unrelated-histories
      - run: git push 
        

    